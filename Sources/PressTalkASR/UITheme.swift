import SwiftUI

enum UITheme {
    static let panelCornerRadius: CGFloat = 16
    static let cardCornerRadius: CGFloat = 14

    static let listeningColor = Color(red: 0.16, green: 0.68, blue: 0.36)
    static let transcribingColor = Color(red: 0.92, green: 0.58, blue: 0.12)
    static let successColor = Color(red: 0.16, green: 0.58, blue: 0.72)
    static let errorColor = Color(red: 0.84, green: 0.28, blue: 0.25)

    static let panelBackground = Color.white.opacity(0.78)
    static let panelBorder = Color.white.opacity(0.82)
    static let secondaryText = Color.black.opacity(0.62)
}
